plugins {
    id 'java'
    id 'application'
}

version = "1.0"

dependencies {
    implementation project(':NewInvokeSpecial - Source')
    implementation "org.spongepowered:mixin:${project.mixin_version}"
}

jar {
    manifest {
        attributes([
                "Automatic-Module-Name"   : "newinvokespecial",
                "MixinConfigs": "newinvokespecial.mixins.json"
        ])
    }
}